import Foundation

/// Non-secret preferences backed by `UserDefaults` (TECH §8.1).
///
/// Engine choice, default target language, launch-at-login, last-sync cursor.
/// Stub for slice 1.
final class SettingsStore {
    // TODO(slice: prefs): typed UserDefaults-backed properties.
}
