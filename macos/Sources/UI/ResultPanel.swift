import AppKit

/// The translation result UI (TECH §8.1).
///
/// An `NSPanel` that becomes key so an agent app can explicitly grab focus.
/// `NotebookWindow`, `PreferencesWindow`, and `OnboardingWindow` will join this
/// folder in later slices. Stub for slice 1.
final class ResultPanel {
    // TODO(slice: result-ui): NSPanel that becomes key + SwiftUI content.
}
